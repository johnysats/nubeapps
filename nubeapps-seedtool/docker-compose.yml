version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: nubeapps-seedtool_web_1
      APP_PORT: 80

  # Un unico servicio: nginx sirviendo el index.html firmado del release de upstream.
  # La app no guarda nada, asi que no hay bind mounts ni usuario 1000:1000 que respetar.
  web:
    image: ghcr.io/johnysats/seedtool:2.3.0@sha256:092e8ca9172f3d8b43166754ce0ba71b47b2029281212610d2b24d7d0c3ed643
    restart: on-failure
    read_only: true
    tmpfs:
      - /tmp
      - /var/cache/nginx
      - /var/run
