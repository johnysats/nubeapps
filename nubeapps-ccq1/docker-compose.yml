version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: nubeapps-ccq1_web_1
      APP_PORT: 80

  # Router: / -> noVNC del simulador, /files -> explorador de la MicroSD.
  web:
    image: ghcr.io/johnysats/ccq1-web:6.6.1QX@sha256:cbb7081172842021695478aa5ac1f18a96b534f9702ef389c5c3abefbfea618b
    restart: on-failure
    environment:
      # Nombres completos de contenedor, no los cortos del compose: todas las apps de
      # umbrelOS comparten la red umbrel_main_network, donde "sim" y "files" resuelven a
      # los contenedores de TODAS las instaladas a la vez.
      SIM_HOST: nubeapps-ccq1_sim_1
      FILES_HOST: nubeapps-ccq1_files_1
    depends_on:
      - sim
      - files

  sim:
    image: ghcr.io/johnysats/ccq1-simulator:6.6.1QX@sha256:2b7097f602f44bc076929978986dd3e0a7f25cff998304078e82875280efa6d5
    user: "1000:1000"
    init: true
    restart: on-failure
    environment:
      # Escritorio virtual: tiene que entrar la ventana del sim (518x853 en +100+100) y el
      # xterm del REPL. Lo que se exporta por VNC es solo el recorte de la ventana.
      SCREEN: 640x1024x24
      # -l = PIN 12-12 sin seed. Sin -l el simulador arranca con una seed de test publica.
      SIM_ARGS: --q1 -l
    volumes:
      - ${APP_DATA_DIR}/data/work:/sim/firmware/unix/work

  files:
    image: filebrowser/filebrowser:v2.44.0@sha256:f63369420687482dfdf252f80ef675f44ca1d1cd3a631261fb424136214fe629
    user: "1000:1000"
    restart: on-failure
    environment:
      # sin auth propia: la app ya esta detras del login de Umbrel via app_proxy
      FB_NOAUTH: "true"
      FB_BASEURL: /files
      FB_ROOT: /srv
      FB_DATABASE: /database/filebrowser.db
      FB_PORT: "80"
      FB_ADDRESS: 0.0.0.0
    volumes:
      - ${APP_DATA_DIR}/data/work/MicroSD:/srv
      - ${APP_DATA_DIR}/data/filebrowser:/database
